import { Disposable, NotificationHandler, NotificationType, ProgressToken, ProgressType } from 'vscode-languageserver-protocol';
export interface ProgressContext {
    onProgress<P>(type: ProgressType<P>, token: string | number, handler: NotificationHandler<P>): Disposable;
    sendNotification<P, RO>(type: NotificationType<P, RO>, params?: P): void;
}
export declare class ProgressPart {
    private _client;
    private _token;
    private _disposable;
    private _workDoneStatus;
    private _title;
    private _message;
    private _percentage;
    constructor(_client: ProgressContext, _token: ProgressToken);
    private begin;
    private report;
    cancel(): void;
    done(): void;
}
